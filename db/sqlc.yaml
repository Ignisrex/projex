version: "2"
sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./queries"
    gen:
      go:
        package: "dbgen"
        out: "../server/internal/dbgen"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
